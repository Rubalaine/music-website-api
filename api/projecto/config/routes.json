{
  "routes": [
    {
      "method": "GET",
      "path": "/projectos",
      "handler": "projecto.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/projectos/count",
      "handler": "projecto.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/projectos/:id",
      "handler": "projecto.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/projectos",
      "handler": "projecto.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/projectos/:id",
      "handler": "projecto.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/projectos/:id",
      "handler": "projecto.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
